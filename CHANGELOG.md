# Changelog

本專案的所有顯著變更將記錄在此檔案中。
格式參考自 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)。

## [2.0.0] - 2026-04-28

### Added

- **批量指令操作**：所有管理指令（`/addadmin`, `/addwl`, `/addcwl`, `/addword` 等）現在支援換行輸入多個項目進行批量處理。
- **Username 全面支援**：
    - 資料庫現在會同步儲存用戶的 `user_id` 與 `username`。
    - 支援直接透過 `@Username` 添加白名單或管理員。
- **自動資訊補全**：在添加用戶時，機器人會嘗試自動透過 Telegram API 查詢並對應缺少的 ID 或 Username。
- **資料庫自動遷移**：實作自動檢測與更新資料庫欄位的功能（如新增 `username` 欄位）。

### Changed

- **指令提取優化**：重構 `get_batch_items` 邏輯，可同時解析訊息文本、文字提及 (Text Mentions) 以及回覆訊息中的用戶。
- **白名單檢查機制**：改進了對標記（Mention）的過濾邏輯，現在能更精確地識別白名單內的用戶名稱。

## [1.1.0] - 2026-04-27

### Added

- **互動式管理按鈕**：在違規通知訊息中加入「忽略」與「永久封鎖」按鈕，方便管理員快速處置。
- **違規回復機制**：
    - 「忽略」功能可將用戶的違規次數減 1，並自動解除禁言或封鎖。
    - 「永久封鎖」功能直接將違規用戶永久封鎖。

### Changed

- **穩定性提升**：在群組管理員檢查邏輯中加入異常處理，避免 API 呼叫失敗導致程式中斷。

## [1.0.0] - 2026-04-27

### Added

- **初始版本發佈**：核心管理邏輯與 SQLite 資料庫整合。
- **自動處置功能**：實作階梯式違規處分（警告 > 10分禁言 > 永久封鎖）。
- **訊息過濾系統**：
    - 連結偵測與阻擋。
    - 違禁詞匹配系統。
    - 非白名單用戶標記限制。
    - 外部頻道轉發與回覆來源驗證。
- **管理指令集**：實作 `/addadmin`, `/addwl`, `/addcwl`, `/addword` 等及其對應的刪除指令。
- **智慧通知**：實作 180 秒延遲自動刪除機器人通知的功能。
- **資料庫架構**：建立 `admins`, `whitelist`, `channel_whitelist`, `banned_words`, `violations` 資料表。
